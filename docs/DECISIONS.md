# Decisions

One row each, outcome only. A decision is taken when the task that needs it starts, and the row is
written in that task's own pull request under the number its release plan pre-assigned. Cite the
task id when the decision came from one.

| | Decision | Outcome |
|---|---|---|
| D1 | A `gateway` module | No. Merged into `data` |
| D2 | Splash | The SplashScreen API, with no minimum hold |
| D3 | `UiState.loading` | Defaults to `null` |
| D4 | Convention plugins | Yes, and they own the shared dependencies |
| D5 | Android Gradle Plugin | Stable 9.x |
| D6 | Navigation 3 | Migrated, with no spike first |
| D7 | Network and database | Ktor client and Room |
| D8 | Crash reporting | An interface with a logging default; no vendor SDK in the repo |
| D9 | Screenshot tool | Compose Preview Screenshot Testing — superseded by D14 |
| D10 | Material 3 Expressive | No. Standard Material 3; its components exist only in an alpha |
| D11 | Design system | Imported from the KSD system as three layers |
| D12 | Dynamic colour | Removed, not defaulted off |
| D13 | Brand face | Source Sans 3, bundled as one variable font |
| D14 | Screenshot tool, second try | Roborazzi |
| D15 | End-to-end tool | Maestro |
| D16 | The component gallery in a release build | No. It lives under the debug menu |
| D17 | Merge policy | One pull request per task, rebase-merged; `main` is one commit per task |
| D18 | Room wiring | `convention.android.room`, applied beside `convention.feature.data` |
| D19 | Template or product | A template |
| D20 | What the sample talks to | Ktor `MockEngine` fixtures on the `dev` flavor |
| D21 | Hardware | A physical device exists and is used for the device-only checks |
| D22 | Licence | None. All rights reserved |
| D23 | Sample features | Favourites, cart, profile and search all stand |
| D24 | Locales | English and Czech, hand-written |
| D25 | Branch protection | Not available on this plan; CI reports, nothing enforces |
| D26 | Renovate | Configured in the repo, the app not installed; parked |
| D27 | Vendor policy | Google, JetBrains and androidx first; then large and widely used. Anything else earns a row here, and if it ships in the release build, a first-party alternative that was tried and found wanting |
| D28 | D27 applied | Koin, Coil, Roborazzi and Maestro stay; mockk and `dependency-analysis` went |
| D29 | Theme | The hand port stands; no token pipeline until drift hurts |
| D30 | Maestro in CI | Weekly and on demand, never per pull request |
| D31 | Versioning | The name comes from the `v*` tag, the code from the commit count; a local build is 1 / `"1.0"` |
| D32 | Store upload | None. A GitHub release carrying the artifact is the release |
| D33 | Icon set | Material icons, in the three sizes `AppTheme.icons.sm/md/lg`. The design names Lucide, which has no first-party Compose artifact |
| D34 | Presentation layout | A directory per screen, even a lone one, and a file per component in the feature's `component/` |
| D35 | Where a screenshot test lives | One per `presentation` module and one in `:core:ui`, cloned from `feature/template` |
| D36 | `ComposablePreviewScanner` | In, under D27's test-only leniency: it reads the `@Preview` functions that already exist, and the alternative is a second list of every golden kept in step by hand |
| D37 | Plan format | Superseded by D41 |
| D38 | Deleting components | Nothing in `:core:ui` is deleted until the showcase features have given the unused ones a home. **Outcome (E3T1):** Trips and Inventory gave thirteen of the sixteen a home; the other three are the chrome's own and stay — see D65 |
| D39 | Open questions in a plan | Answered at the task, not up front. A task that carries one has a `Decide first` line with the recommended reading first |
| D40 | `doctor.py`'s weaker checks | All of them stay. The pruned walk took the suite to a quarter of a second, so the cost argument is gone; reopen on taste, not on time |
| D41 | Documentation system | Work is planned as releases of point-estimated tasks with four-character ids, and checks are tiered T0–T4; `CLAUDE.md` holds the rules alone. Replaces D37's single plan file. Its five zones by kind are superseded by D48 |
| D42 | The 2026-09 retrospective | Deleted with A0P2 rather than archived — the archive's only purpose was to hold it until release A shipped, so there is none. A task that came from it says so once, as `(was F12)` |
| D46 | The token-refresh scaffold | Kept. It is built on Ktor's `bearer` provider, its stale-token comparison reads correctly and it is tested; nothing supplies it yet only because there is no real API. Deleting working, tested concurrency code to be rewritten later is a worse trade than carrying it |
| D47 | How release A ships | One pull request for the whole release, not one per task — the release is a cleanup, and 19 pull requests would be 19 rebases of the same tree. For release A only: D17's one-per-task stands from release B on |
| D48 | The documentation tree | Sorted by depth of audience, not by kind: six files directly under `docs/` for a human, everything else under `docs/ai/`. `ai/` means AI-*only*, not AI-*all* — the agent reads the whole tree, and audience decides how deep a file sits, never what it contains, so a human edition and a machine edition of one fact cannot be written. Everything stays under `docs/`, which is what CI's documentation-only test matches on |
| D49 | The module topology | Both splits stay, and the packages under `service/` move to match them. The ten `di` modules are the only modules allowed to see all four layers, and folding one into its `presentation` would put `data` on a presentation classpath — the rule the template exists to demonstrate. They cost nothing worth having: median cold configuration over five runs each was 1.78 s with their plugins and 1.73 s without, and 0.7 s either way once the configuration cache is warm, so this is D40's answer again — reopen on taste, not on time. `service/` keeps its directory split, which is what `check_service_isolation` enforces and what the export recipe copies, but its Kotlin packages move from `…core.*` to `…service.core.*` (and `…core.network` to `…service.network`). The defect that fixes: `…core.ui.component` existed in both `:core:ui` and `:service:core:ui`, one package split across two modules, so an import said nothing about where a symbol came from and `internal` behaved in a way nobody expected. Taken in B0P1 |
| D44 | `BaseViewModel`'s defaults | The loading overlay becomes **opt-in** — `loading = overlay()`, or `overlay(message)` to word it — because 24 of the 32 call sites in the app switched it off, and a default three call sites in four override is not a default. `loadingMessage` goes with it: a wording parameter separate from the switch could be set on a call that showed no overlay to put it on. `initialState` **stays nullable**. The measurement that decided the second half: three of the four screens that pass `null` have a natural empty state, and the product detail screen does not — its state holds a `Product`, which has no honest empty value, so a non-null envelope would move the null into the feature's own `@Immutable` state class, where `Screen()` no longer guards it and every preview and golden has to render it. That is the same null one level down, unguarded. What the null half was really for — an update arriving before the first state being dropped in silence, which is the shape of the bug A1X4 fixed by hand — is fixed directly instead: `updateData` is now a member of `BaseViewModel` that logs that case. Taken in B0X1 |
| D50 | How `Screen()`'s chrome joins the design system | A `ScreenChrome` interface in `:service:core:ui`, carried by `LocalScreenChrome` and installed by `AppTheme` — not slots threaded from `AppNavHost`. The five things the shell draws (base surface, loading overlay, empty and error content, alert dialog, snackbar host) were raw Material with `dp` literals, which is the rule `CLAUDE.md` § Design system puts on every feature and exempted itself from; `:service:core:ui` may not name `:core:*`, so it could not read `AppTheme` to fix it. The composition local costs nothing at either end: `:core:ui` already depends on the module and every screen already composes inside `AppTheme`. `DefaultScreenChrome` keeps the Material rendering so `export_service.py` still produces a directory that runs on day one, and it is the only place in the repository allowed `dp` literals in a screen's chrome. Slots on `AppNavHost` were the alternative and were rejected for putting another lane's file in this one. Taken in B1U1 |
| D51 | `AppTextField`'s API | **Keeps `value` / `onValueChange`** and gains what it was missing: `imeAction`, `onImeAction` and `contentType`. Rebuilding on `TextFieldState` was the recommendation and is wrong for this template: a `TextFieldState` is mutable and observable, so the field's text cannot live in an `@Immutable` `XState` — it moves into the composable, and with it the validation every form view model here does on change. That is the one thing an MVI template exists to demonstrate not doing. Revisit when Compose's own guidance for a state-hoisted architecture settles; the missing parameters were the actual defect and cost three lines. The two size enums are folded in the same change: `ControlSize` is the one scale, and `ButtonSize`'s taller metrics become a private lookup inside `AppButton` — the goldens are byte-identical afterwards, which is what proves the fold changed nothing. Taken in B1U2 |
| D52 | Where the gallery's list comes from | **Hand-written, and checked.** `ComposablePreviewScanner` was the recommendation and does not survive contact: it is a classgraph-based scanner built for a JVM test classpath, and the gallery ships in `dev` and `staging` (D16), so it would have to run on a device. The deeper reason is that it would make the gallery worse — an entry carries several labelled variants and live, interactive demos, and a `@ComponentPreview` renders one fixed arrangement, so generating from previews would trade 46 rich entries for 46 thin ones. What was actually missing was enforcement: `create_component.py` now writes the starter entry and `doctor.py`'s `check_gallery_lists_every_component` fails on a component with none, or on an entry naming a component that does not exist. The brief's premise that two components were already missing was wrong — the gallery was complete; it was just complete by luck. `AppScaffold` and `ControlSize` stay out for the reasons the catalogue's own KDoc gives. Taken in B1U3 |
| D43 | The `staging` flavor | **Kept, alongside `dev` and `prod`.** It costs two small source-set files and one line in `ProjectConfig.Flavor`; the pull-request gate only compiles its Kotlin, not assembles it, so the surface that once justified cutting it is gone. A template's job is to show the shape a real app has, and three environments — with an application-id suffix so all three install side by side — is that shape; the two source-set files are the demonstration, not overhead. D16 already names the gallery as living under the debug menu without naming a flavor, so it needs no rewrite. No code changed: `ProjectConfig.Flavor`, `CLAUDE.md` § Commands, `docs/ai/ARCHITECTURE.md` § Environments and the build workflow already agreed with this outcome. Taken in B2P1 |
| D53 | Where the data-source interface lives | **Kept, and two got a fake.** Deleting the ten interfaces would make `create_datasource.py` simpler and the data layer thinner, but it also removes the only seam a network or file-system failure can be injected through without a real client or a real filesystem — and the fakes are the cheaper half of that argument, so wrote them instead of deleting what they'd replace. Picked the two data sources with a real failure branch: `RemoteCatalogDataSource` (an HTTP call, mapped through `HttpErrorMapper`) and `AvatarDataSource` (a file copy that can fail on a revoked URI). `FakeRemoteCatalogDataSource` ships as available `testFixtures` infrastructure — `DefaultCatalogRepositoryTest` deliberately keeps its own `MockEngine` because cache-then-network is the thing *that* test asserts, so faking the network there would defeat it; the fake is for a narrower test that wants the failure without one. `FakeAvatarDataSource` got an immediate consumer: `DefaultProfileRepositoryTest`, which did not exist before this task — `DefaultProfileRepository` had no test at all. Both fakes needed AGP's `testFixtures` source set turned on for an Android library module, which the plain `java-test-fixtures` Gradle plugin cannot do (it collides with a configuration AGP already owns) — a new `convention.android.library.testfixtures` plugin does it instead, stacked alongside each module's existing convention plugin. `check_repository_depends_on_interface` needed no change; it already passed. Taken in B2P2 |
| D45 | The baseline profile | **Cut.** `:baselineprofile`, `convention.android.test` (its only consumer) and both plugin aliases are gone, along with the committed profile and the now-unused instrumentation libraries. Measured before deciding, without a device — none is attached in this environment, and no CI job ever ran the generation either, which is itself part of the finding: nothing has regenerated this profile since it was first committed. What could be measured is whether the committed `devRelease` profile does anything for `prodRelease` if pointed at it directly, since each flavor's R8 pass renames classes independently: built both bundles, and copying the profile to where AGP's native `src/main/baselineProfiles/` convention picks it up for every variant grew `prodRelease`'s embedded `baseline.prof` by 10 bytes out of roughly 9,900 — the recorded rules reference `devRelease`'s own obfuscated names and essentially none of them match `prodRelease`'s independently-renamed classes. A correct fix needs either a same-build mapping-rewrite between the two variants' R8 output or a separate device-generation run per flavor, well past what a two-plugin, one-consumer dependency should cost, and the benefit is unverifiable here regardless: this is a template with no startup-heavy real work to compile ahead of time, and the shipped build has carried none of this since v1.0 with nobody noticing. `docs/ai/DEPENDENCIES.md` loses its baseline-profile row. Taken in B2H2 |
| D58 | How many plans may be open | **Exactly one is open; the rest queue as `draft` or `paused`.** A third state was the cheaper half of the problem: release D had to run before B finished, and the alternative — two plans carrying `Status: open` — would have made "the plan whose header says `Status: open`" a question with two answers, forcing a tie-break rule into `/task`, `/release`, `PROCESS.md` and `CLAUDE.md` that none of them needs. `Status: paused` keeps that sentence true everywhere it is written, costs one word in a header, and `STATUS.md` keeps the paused release's board under its own heading so seventeen merged tasks are not cut to make room for nine unstarted ones. The second half: the 90–100 band sizes a **lane**, not a release — `ceil(60 ÷ 100)` is one lane either way, and "none is under 85" was written to stop a four-lane release being cut into five thin ones, not to set a floor under a release. A 60-point release is one lane under the floor, and the neighbouring rule — never pad a lane — says what the alternative would have cost. The budgets rise to 400 and 160 and move to `docs/README.md` alone, where they were already written a second time: a line count is a proxy for "this doc has started explaining itself", and the proxy was being enforced against the thing it stood for. Taken in D1P1 |
| D59 | Which feature gets the fifth tab | **Trips, and release C's arcade hub finds another entry point.** B3S1 built `TripsScreen` as a tab root — `AppScaffold` with no `onNavigateUp`, and a comment saying the bottom bar is what leaves it — and then shipped without the tab, so five screens, thirty source files and a Room database were in the APK with nothing able to open them. That shape is what decided it: the other four trips screens each carry an up arrow and are reached from `TripsScreen`, so one tab entry unlocks the feature and a push from anywhere else would strand the user on a screen with no way back. The cost is real and is paid knowingly: Material's bottom bar tops out at five, so the slot is single-use, and `C0P1` had already drafted D55 to spend it on the arcade. C is a draft and unstarted, its own D55 text offers "a row on Home" as the alternative it weighed, and a component gallery for developers is a better fit behind the dev menu than beside the cart — whereas Trips has no second home, because it is a tab root by construction. `C0P1` is not amended here; C is edited when it is drafted for real, and `STATUS.md` § Next carries the note. Two secondary facts went with it: `BACKLOG.md` and `reference/FEATURES.md` both said the tab was skipped because the enum and `:app`'s strings were "outside that lane's file set" — `B.md`'s Shared files table gives all of `app/**` to lane 3, so they were inside it; what they were outside of was B3S1's own `Touches` line. And `FEATURES.md` cited D47 for it, which reads "For release A only". Taken in D1X1 |
| D60 | The test-id vocabulary | **Stays closed, gains five content kinds, and the component-named tags are renamed — not widened to fit.** Measured first: 101 tags across the feature presentation modules, and the "nineteen violations" the task was written from is really 18, because 14 of the apparent offenders — `cart_item`, `categories_list`, `tripDetail_tabs` — are the vocabulary words `Item`, `List` and `Tab` in the lowercase a single-word element gets from camelCase. They always conformed; the reading of the rule was wrong, and any check has to fold case and plural before judging. Of the 18 that are real, 13 name a *kind of content* the existing thirteen words cannot express — an empty state, a skeleton, a card, a progress bar, a grouped section — and 5 name a *component*: `quantityStepper`, `budgetSlider`, `typeSegmented`, `advancedAccordion`. That split is the decision. Adding all eleven missing suffixes would take the list to 24 and turn a vocabulary into an inventory of `:core:ui`, which destroys the only property that makes it worth having: that you can guess `settings_permissionsButton` without opening the file. So the list gains `Card`, `Empty`, `Skeleton`, `Progress` and `Group` — kinds, not components, and each earns its place by describing something no existing word does — and the five component-named tags become `Field`, which is what a stepper, a slider, a segmented control and an accordion are when a form holds them. `cart_emptyState` and `trips_emptyState` lose the redundant `State`, and `tripWizard_reviewSummary` becomes a `Tile`. Eighteen words, and `check_test_id_vocabulary` enforces them. Taken in D1X4 |
| D61 | How many agents work a release | **One, lane by lane; two or three only when the owner writes it in the plan's `Agents:` line, for a release whose lanes are different kinds of work; never more, and never subagents or workflow scripts inside a session unless the owner asks for them by name.** The lane model was built for parallel agents — a lane was an agent-day, and the planner lowered the agent count until every lane was 85–100 points — and it cost what it saved: every agent is a full context, roughly 100k tokens read before its first edit, so four agents on a 380-point release paid 400k tokens of overhead to save a calendar day nobody had asked for. A lane is now a group of related work with a file set no other lane writes, as long as its work, worked in order by one agent; the disjoint file sets are what let a second agent take a lane if the owner wants the day back, so the Shared files table stays. The pull request follows the lane, not the task — one per lane, every task one commit on it, rebase-merged — so D17's invariant, `main` is one commit per task, stands with a fraction of the pull requests; D47 was the same shape for release A alone and is now the rule. Taken in E0P1 |
| D62 | What the plans B, C and D become | **B and D close together as v1.1.0; C is deleted; the backlog is groomed once and its top two thirds are release E.** Three plans were in flight — D open, B paused at its last task, C a 380-point four-agent draft — and each carried leftovers the next would have inherited. B3S2, Field report, is dropped rather than paused: new development whose platform seams have no user yet, so it is one backlog line under its old id. C, the arcade, is abandoned: nine games were an entertaining reason to compose sixteen homeless components, and one plain feature — a list, a detail, a four-step editor with create, edit and delete — composes the same sixteen in the shape a real app has, which is what a template is for. That feature is Inventory, release E's lane 3, reached from a card on Home because Material's bottom bar tops out at five and Trips holds the fifth (D59). D1X2 and D1X4 leave D — the first to the backlog, the second into E as E1X1 — so D can close; both were marked "blocked: no JDK 25 toolchain", which was true of the sandbox they were attempted in and not of the repository, whose daemon provisions that JDK. What E does not take is ranked in BACKLOG.md § Next and is not carried forward by default: the next grooming does each line or deletes it. Taken in E0P1 |
| D63 | What a toast is | **A snackbar with no action; `UiCommand.ShowToast` and the Android `Toast` are gone.** `Screen()` drew a window-level `Toast.makeText` for every `ServerError` and three feature calls — the one thing D50 left outside the chrome, so it ignored the theme and no screen test could see it — while the snackbar already rendered through `AppToast`. Two mechanisms for one job. All four call sites speak to the screen they are on, and a message that must outlive its screen is a feature nothing here has, so the shell keeps one: `showToast(message)` is `showSnackbar(message)`, and a `Toast` slot in `ScreenChrome` is not added. Taken in E1X2 |
| D64 | What a module build file may hold | **A `plugins` block, its project dependencies, and `resourcePrefix` — nothing else.** `resourcePrefix` is per module by nature: it is the one Android setting whose value is the module's own name. Everything that was there besides — sixteen library lines and a hand-written `testFixtures { enable = true }` in `service/core/ui`, three libraries in `service/core/data`, one in `service/core/domain`, one in `core/ui`, three in `app` including a `devImplementation` — moves into a convention plugin named for the module (`convention.service.core.{ui,data,domain}`, so a copied `service/` brings its build with it), into `convention.core.ui`, or into `convention.android.application`, which already iterates `ProjectConfig.Flavor` and so owns the flavor-specific line. `check_module_build_files_are_thin` fails on a `libs.` reference outside `plugins { }` and on a `testFixtures { }` block, so the rule holds without being remembered. Taken in E2P1 |
| D65 | The sixteen homeless components, one by one | **Thirteen found a home in Inventory, three are the chrome's own and stay, none is deleted.** The scan of 2026-09-13 named sixteen `App*` files composed by nothing outside `:core:ui` and the gallery. Re-run after E3S6: `AppAvatar`, `AppBadge`, `AppCheckbox`, `AppFormField`, `AppImage`, `AppRadio`, `AppSelect`, `AppSlider` (the list, the editor and the detail); `AppBottomNav` and `AppNavRail` (the detail's sections, chosen from `SizeClass`); `AppSheet`, `AppSpinner` and `AppToolbar` (the list's filter, pending rows and selection mode) — thirteen, each with a real job on a real screen rather than a demo. `AppScreenChrome` stays because `AppTheme` installs it and no feature may name it (D50); `AppDialog` and `AppToast` stay because the chrome draws every alert and every snackbar through them — the scan cannot see that use because it looks outside `core/ui`, which is the right place to look for everything else. The rule stands: a component nobody composes teaches someone to compose it, so the next one still homeless after the next showcase is deleted, not deprecated. Taken in E3T1 |
| D66 | How work is framed in time | **Sprints, and a sprint is a release.** The release machinery — ids, bands, lanes, the task loop, `/release` — works and nothing in it bounded a release in time: E ran until its backlog ran out, and the day's first question, "what can we do today", had no file to answer it. The owner asked for scrum. What it costs to adopt is one table in `PROCESS.md` mapping the vocabulary onto what already plays each part — the product backlog is `BACKLOG.md` § Next, planning is `/release draft` plus the owner opening it, the sprint goal and timebox are one `Sprint:` header line, the board is `STATUS.md`, review is `/release close`, the retrospective is the ratio — and what a second process beside the first would cost is two boards, two loops and a tie-break rule in every command. The timebox is one agent-day unless the owner writes otherwise, because 100 points is already the unit the bands are calibrated to. A task open when the timebox ends returns to § Next under its old id and is not carried by default, which is D62 applied every sprint rather than once. Field report (B3S2) is deleted from the backlog outright: it waited on purpose through two groomings and nothing has come to need its seams. Taken in F1P1 |
| D67 | Where the sprint is planned, and where it is read | **The sprint replaces the lane, a release collects sprints, the repository stays the truth, and a board artifact is the window on it.** Three things the owner asked for on 2026-09-15 and one constraint. *One sprint at a time:* the lane digit in a task id becomes the sprint digit — the pattern `^[A-Z][0-9][UXTHPS][1-9]$` does not change, so `doctor.py`, the changelog and every old id keep working — and a sprint is one branch, one pull request and one file, `plans/<letter><n>-<slug>.md`, with a name, a `When:` and a `Goal:` a person can read. Lanes existed so a second agent could take a file-disjoint group; a sprint's § Files table keeps that property per task, and D61's one agent makes the lane a digit nobody used. *A release is not a sprint:* `plans/<letter>.md` opens, collects done sprints, and ships as one changelog block when the owner says — after `/sprint close` the default is another sprint, never a tag, because a two-hour sprint is not a version. *Drafts are the queue:* the first `Status: draft` file listed in `STATUS.md` § Drafts is the next sprint by default. *The board can be read from anywhere:* a claude.ai artifact with a `db` store holds one document, `board/state`, written by `/board` from `scripts/board.py`'s output — a parser over `STATUS.md`, `BACKLOG.md`, `CHANGELOG.md` and `plans/` and nothing else. The constraint is the one this repository already lives by: a fact lives in one file. The artifact is never edited by hand and never written from the page; if it and `STATUS.md` disagree, `STATUS.md` is right and `/board` fixes the artifact. The alternative — the store as the truth and the docs generated from it — would put the sprint's briefs, which every task reads and every commit flips, behind a tool call. `BACKLOG.md` gains a group per line and a § DevOps section, so a board can be read by layer and an improvement sprint has somewhere to draft from. Taken in F1P1 |
| D68 | Where the connectivity monitor lives | **The interface in `service/network`, the Android implementation in `:app`, and on `dev` the answer is the offline switch.** `service/network` stays the Kotlin/JVM module it is: making it an Android library to reach `ConnectivityManager` would cost every other consumer the JVM test speed and break `export_service.py`'s promise of a directory that compiles anywhere, for one class. The seam already exists — `HttpClientFactory` takes its engine as a parameter and `:app` hands it OkHttp or fixtures per flavor — so the monitor takes the same road: a port beside the engine, `AndroidConnectivityMonitor` in `:app`'s `src/main` where a Robolectric test can reach it, and a `connectivityMonitor()` per flavor source set beside `networkEngine()`. On `dev` the engine is in-process, so airplane mode means nothing to it; the one truth for "the server is down" there is `FixtureNetwork`, which the debug menu's switch already writes, so it implements the port itself and the banner follows the switch. The retry policy reads the same flow and gives up at once while offline: three tries on a device with no route are three identical failures and two seconds of backoff in front of an answer that was known before the first. The banner is `AppBanner`, not a toast (D63): nothing happened, something is so, and it stays until it is not. Taken in F1H1 |
| D69 | How a backlog line is tagged | **The group is a module path at any depth or a process area, the kind is the id letter the line will get, and a section says how ready a line is — never what it touches.** Every line in every section is `- <title> · <pts> · <group> [· <kind>] · <why>`; `pts` is a band or `?`, and `?` is allowed only under § Someday and § Behind a decision. A module path — `core:ui`, `feature:auth:data`, `service:network`, `app` — names what the work touches, so `board.py` derives `area` (the first segment), `feature` (the segment after `feature:`) and `layer` (a trailing `presentation`, `domain`, `data` or `di`) from one string, and a module that does not exist yet can be named the same way; a process area is one of `build`, `ci`, `release`, `git`, `process`, `templates`, `claude` or `tests`. The alternative — a second tag vocabulary beside the module tree — would be one more list to keep in step with the module tree, and its first drift would be invisible. The kind is the `UXTHPS` letter of § Ids, given where it is known; a bug is kind `X` with a `seen on <flavor> <version>, expected <what>, steps <n>` clause. `doctor.py` holds the grammar (F2P1) |
| D70 | Who republishes the board when no session does | **A scheduled cloud routine, each weekday at 07:00 Prague, from the open sprint's branch — and the page says how old it is.** The constraint is fixed: the artifact store is written through the Artifact tool only, so a workflow step cannot do it. A routine can: *AndroidProject1 · /board each weekday morning* (`/schedule`, `0 5 * * 1-5` UTC, `claude-sonnet-5`, the Default environment, tools Bash/Read/Glob/Grep/Artifact, no connectors) checks the repository out, runs `board.py`, reads `board/state` for its version and writes it back with `if_version`. Three things its first runs taught, each now in its prompt: the JSON must lie inside the checkout — a file under `/tmp` raises a permission nobody can grant, so it goes under `.git/`, where `git status` does not look; the session needs `permission_mode: auto` (a `set_permission_mode` control event before the prompt) or the write waits on an approval forever; and it publishes the open sprint's branch — the newest `<letter><n>-*` on origin that is ahead of `main` — because a sprint's board lines flip on its branch and reach `main` only when it merges, so a routine reading `main` would reset a mid-sprint board every morning. Run 4 wrote version 12 from `F2-one-backlog-one-board @ a838dc7` in 24 seconds with no session involved, a clean `branch` and `dirty: false`. The fallback — the board is as fresh as the last `/board` — stays true between runs, which is why `syncedAt`, `branch` and `commit` are in every document and F2P5 draws the age (F2P4) |
| D71 | Whether the repository is public | **Public, and CI stays the gate as `CLAUDE.md` § Checks writes it.** GitHub Actions refused every job from 2026-09-13 to 2026-09-16 — "recent account payments have failed or your spending limit needs to be increased" — so sprints F1 and F2 merged on the local gate, and the owner will not pay for GitHub. The alternative, private with T2–T4 struck from CI and run by hand before a merge, keeps the fixtures out of sight and loses the three things CI alone does: a run on every push to `main` that nobody has to remember, the weekly T4 with the release build and the flows on an emulator, and a record a pull request can point at. Public costs nothing that was secret — the history scan before the flip found no keystore, no properties file and no token, only a source file whose name contains "keystore"; the signing secrets are repository secrets and were never in the tree; the `dev` flavor talks to `MockEngine` fixtures (D20) — and a template exists to be read. The rerun of #29's run went green on the jobs billing had refused within a minute of the flip. What public also unlocks is a ruleset on `main` (F3P2). A run that never started stays what F2P3 made it: `not started: <reason>`, never `fail`, and never green. Taken in F3P1 |
| D72 | Who pushes the tag | **`/release close` pushes it, in the session that merged the ship pull request, on the owner's word — never a workflow.** `v1.2.0` had a changelog block for three days and no tag because the close ended at "the owner pushes the tag": a step that ends in someone else's hands is a step that does not happen, and the audit flagged it at every sprint close. The alternative — the release job tags `main` when a `## v<x.y.z>` block lands with no matching tag — fails on a GitHub rule: a tag pushed with `GITHUB_TOKEN` triggers no workflow, so the job would have to build and publish the release in the very run that tagged, which folds the release job into a push-to-main job and loses the one property a tag has, that it is deliberate. A personal token would fix that and is a secret with a lifetime to manage. So the command does it: the ship is the owner's word, the merge is the moment, `git tag v<x.y.z> origin/main` is the commit, and the close is not done until `gh release view` answers. The check stays where it was — the audit's *a tag for every shipped block*. `v1.2.0` is tagged at `70de4f3`, the ship commit, in F3P3. Taken in F3P3 |
| D73 | Whether CI is the gate | **Not for now: `build.yml` stays in the tree and is disabled, the `main` ruleset requires no status checks, and `/check pr` on the machine that merges is the whole gate.** The owner's word on 2026-09-17: Actions is a paid feature for this account — a few builds a month get checked, then every job is refused on billing — and they will not pay for it; D71's public flip bought one afternoon of runs and is not something to depend on. Leaving the workflow enabled would mean every pull request shows a red or a never-started check that nobody may act on, and a ruleset that requires those checks would merge nothing. So the workflow is turned off by hand (`gh workflow disable build.yml`), not deleted or commented out — the file is the record of T2–T4 and runs unchanged the day it is enabled again — and the ruleset keeps what does not depend on a run: a pull request, a rebase merge, no force-push, no deletion. What is lost is named rather than papered over: nothing watches `main` after a merge, the weekly release build and Maestro flows do not run, a tag makes no release page, and the coverage line F3P6 wrote goes nowhere. The pull-request body's `/check pr` tail is now the record the sprint-close audit reads. Turning it back on is one § DevOps line, the owner's call. Taken in F3P9 |
| D74 | Branching | **Gitflow, on the owner's word (2026-09-17): `main` is shipped code and tags, `develop` is where sprints land, a task is `feature/<id>-<slug>` into `develop`, a ship is `release/<x.y.z>` into `main` and back, a hotfix is `hotfix/<id>-<slug>` from `main`; every merge is `--no-ff` through a pull request.** This supersedes D17's rebase-merge and its "one pull request per lane, `main` is one commit per task": with merge commits, `develop` reads as one task commit and its merge per task, and `main` reads as one merge per release — which is what a release log wants and what `git log --first-parent main` prints. What it costs is one pull request per task rather than per sprint, and the two rulesets are edited to allow merge commits (rebase and squash stay refused, so a feature branch's one commit is kept as written). `develop` was cut from `main` at `b3f5901` and made the default branch, so a pull request opens against it unless told otherwise. Taken in F0P2 |
| D75 | Where a per-app language is applied | **`AppCompatDelegate.setApplicationLocales` through an `AppCompatActivity`; `LanguageRepository` is an interface in `:feature:settings:domain` and `AppCompatLanguageRepository` in `:app`, the way D68 placed the connectivity monitor.** `minSdk` is 29 and the platform's `LocaleManager` arrives at 33, so a picker that only worked on the platform API would be hidden on the emulator F3P7 boots; AppCompat is the compat layer Google ships for exactly this and it is androidx, so no decision row for the dependency itself. Below 33 the stored locale is applied in `attachBaseContext`, before the first frame, and only by `AppCompatActivity` — which is why `MainActivity` changes its base class and `Theme.AndroidProject1` its parent (`Theme.AppCompat.DayNight.NoActionBar`; the window is all it styles, the app's colours stay in `:core:ui`). `AppLocalesMetadataHolderService` with `autoStoreLocales` is the store: AppCompat persists the choice itself and reads it back synchronously, which a DataStore-backed repository could not do before the first frame without a blocking read — so nothing is written to DataStore, and the repository is a thin port over `getApplicationLocales()` / `setApplicationLocales()` with a `StateFlow` re-read after every set. The implementation lives in `:app` rather than `:feature:settings:data` because `AppCompatDelegate` is an activity-stack class: putting it in a data module would add AppCompat to `convention.feature.data` for every data module, for one class. A locale-config resource names the two shipped locales, so the API 33+ system settings list the app too. The alternative — `LocaleManager` on 33+ and the row hidden below — was rejected for the reason in the first sentence. Taken in F4U1 |
| D76 | How a screen takes part in a shared-element transition | **A `:core:ui` modifier — `Modifier.appSharedElement(key)` — over two composition locals the host provides, and a no-op wherever they are absent.** Compose's shared elements need a `SharedTransitionScope` around the whole `NavDisplay` and the entering entry's `AnimatedVisibilityScope`, and navigation3's own local for the second throws outside an entry, so a preview, a Robolectric test and the gallery could never compose a screen that read it. So `:core:ui` owns both locals as nullable, `AppNavHost` wraps `NavDisplay` in `SharedTransitionLayout` and re-provides the entry scope from navigation3's local inside each entry, and the modifier applies `sharedBounds` only when both are present — the same screen draws unchanged everywhere else, which is what keeps the goldens identical. The scope is provided on a compact width only: on a wider window the catalog's list and detail are one scene, both visible at once, and two elements with one key in one scene is not a transition. The motion is the theme's `screenMillis`, so a re-brand that slows screens slows this too. The alternative — the catalog feature opting into `androidx.compose.animation`'s scopes itself — puts the widget set the rules keep out of a feature into one, and every feature that wanted the effect would repeat the guard. The product name and its price are the two shared elements, from the row to the detail's title and figure; `AppListItem` and `AppTopBar` each gain one modifier parameter for the text they draw so a caller can tag it without the component knowing why. A showcase (kind `S`), not a rule: nothing else in the sample uses it. Taken in F4S1 |
| D77 | How the playground knows a component's knobs | **A component's knobs are data — a sealed `Knob` with four shapes, a `KnobValue` per shape, a `render` lambda that reads them — and the screen generates its controls from the knob's shape, never from the component.** `PlaygroundEntry(id, name, knobs, render)` sits in the dev-menu feature's own `PlaygroundCatalog` the way the gallery's catalog sits in the gallery: the gallery is another feature's `presentation`, which this one may not import, and a catalog that reused it would be a dependency the rules refuse for a dev tool. The four shapes are the four controls the design system has for a value — `Toggle` is an `AppSwitch`, `Choice` an `AppSegmented` (an `AppSelect` past four options), `Text` an `AppTextField`, `Number` an `AppStepper` — so the one `when` in the module is over `Knob` and lives in the `PlaygroundKnob` component; the screen holds a `Map<String, KnobValue>` and an entry id and knows no component by name. Adding a component to the bench is one `entry(...)` with its knobs and a lambda that reads them, and no screen or test changes. The alternative — a composable per component with its own controls — is the `when (component)` the backlog line forbade, and it is what the gallery already is: a list of states its author thought of. The stage is a display, not a second control: an entry's own callbacks are empty and its state is the knobs' — a switch on the stage is turned by the `checked` knob under it, which is the point of a bench. Eight entries to start, the controls with the most states; dev and staging only, behind the debug menu, like the gallery. Taken in F4U2 |
