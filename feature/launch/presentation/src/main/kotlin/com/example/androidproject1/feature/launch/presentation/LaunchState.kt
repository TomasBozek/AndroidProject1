package com.example.androidproject1.feature.launch.presentation

/**
 * No fields: this screen only ever shows a spinner. MainViewModel — not this ViewModel — decides
 * when the app is ready to leave it, so there is nothing real to load into a data class here.
 */
object LaunchState {

    val PREVIEW = this
}
