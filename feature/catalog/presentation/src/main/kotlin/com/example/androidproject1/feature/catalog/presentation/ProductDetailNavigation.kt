package com.example.androidproject1.feature.catalog.presentation

/** Empty on purpose: this is a leaf screen, and the system back button already leaves it. */
sealed interface ProductDetailNavigation
