package com.example.androidproject1.feature.example.presentation

data class ExampleState(
    // TODO: This is a mock. Replace with real properties.
    val foo: Int,
) {

    companion object {

        val PREVIEW = ExampleState(
            foo = 1,
        )
    }
}
