<?xml version="1.0" encoding="utf-8"?>
<!-- Every locale the app ships strings in — `values` and `values-cs`. A new `values-xx` adds a line
     here, or the system's per-app language settings will not offer it. -->
<locale-config xmlns:android="http://schemas.android.com/apk/res/android">
    <locale android:name="en" />
    <locale android:name="cs" />
</locale-config>
