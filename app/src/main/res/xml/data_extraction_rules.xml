<?xml version="1.0" encoding="utf-8"?><!--
   Backup and device-transfer rules for API 31+.

   The DataStore file holds the session, so it is excluded from both: neither a cloud restore nor a
   device-to-device transfer should carry credentials across. See also backup_rules.xml, which does
   the same for API 30 and below.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude
            domain="file"
            path="datastore/" />
    </cloud-backup>
    <device-transfer>
        <exclude
            domain="file"
            path="datastore/" />
    </device-transfer>
</data-extraction-rules>
